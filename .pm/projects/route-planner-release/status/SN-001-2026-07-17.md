---
id: SN-001
slug: 2026-07-17
title: Status — 2026-07-17
project: route-planner-release
date: 2026-07-17
created: 2026-07-17T13:31:20Z
updated: 2026-07-17T13:31:20Z
rag:
  overall: green
  schedule: green
  scope: amber
highlights:
  - "GO decision from the M-001 UAT meeting: live release Monday 20 Jul"
  - "All meeting release-blockers built and in review: full addresses (T-0601), half-luton option (T-0221), run weight/volume figures (T-0604), solver second-run pricing — 5 double-runs → 0 on the test day, £190 cheaper (T-0603), job-minutes investigation + unload-minutes bug fix (T-0606), failed collections on the map with self-return/extended filtering (T-0600), per-piece item lists at sketch time (T-0602)"
  - "Release hardening built: same-day finalise typed confirmation with server-side guard (T-0611), conservation audit command `php yii planner-audit/date` + finalise decision logging (T-0612)"
  - "Bug fixes from live testing: map missing routes on live-loaded plans (geocode endpoint referenced a non-existent class), phantom £-159 run cost (stale costings leak + finalise never writing run times/miles)"
  - T-0371 (inactive runs copied) verified already-fixed with an empirical sandbox test
next_two_weeks:
  - "Weekend: run the manual destruction-test checklist + conservation audit after every finalise (checklist in repo doc RELEASE-PLAN-2026-07-20.md)"
  - "Close the two open decisions: finalized-plan drift (re-solve on a finalized date overwrites the finalized sketch without re-stamping to draft — T-0612 finding) and split job-minutes behaviour (proportional vs full per piece — T-0606)"
  - "Monday (gated on Austin): merge PickingSketchSalesDashFriday → master via scratch branch, RDS snapshot, deploy, `php yii migrate` (8 expected migrations), RBAC check for logistics users, post-deploy validation — full steps in the release plan doc"
  - "Week 1: daily conservation audit on live, sketch-planner.log monitoring, drive-time feedback collection; fallback is the unchanged run planner"
risks_summary: "Main release risks and their mitigations: (1) silent job/item loss at finalise — mitigated by the conservation audit (run after every finalise) and the finalise decision log; (2) a good plan being overwritten on delivery day — mitigated by the typed same-day confirmation with server enforcement; (3) finalized-record drift after re-solve-on-finalized-date — detection in place, prevention decision pending; (4) merge risk into a moved master — mitigated by scratch-branch merge with lint sweep and explicit go-ahead gates. Solver box is already live and byte-matched to the branch, so the deploy carries no solver risk."
milestone_progress: "Sprint SPR-001: T-0524/T-0527 done; T-0221, T-0371, T-0383, T-0505, T-0506, T-0600-T-0604, T-0606, T-0611, T-0612 in review awaiting Austin's close; T-0401 (progress bars) deferred past release."
author:
  kind: agent
  name: claude-code
version: 1
---

# Release plan — Monday 20 Jul 2026

The full gated plan lives in the repo: `backend/views/route-planner/RELEASE-PLAN-2026-07-20.md` (branch `PickingSketchSalesDashFriday`, commit 6f3d21ae). Summary:

**1. Weekend testing (sandbox)** — manual destruction checklist (workflow, splits/items conservation, two-person concurrency, deliberate destruction attempts, visibility features) with `php yii planner-audit/date` run after every finalise. Two open decisions to close: finalized-plan drift handling and split job-minutes policy.

**2. Merge (GATE: Austin's go-ahead)** — scratch branch `release-merge-20260720` off current master; merge the release branch there; expected conflicts limited to YaRuns.php / run-planner.php / config main.php (Xero files untouched by the branch); lint sweep; fast-forward master and push only on explicit approval.

**3. Deploy (GATE: Austin present, suggested Mon 07:00)** — RDS snapshot; `git pull`; `php yii migrate/new` preview (8 expected branch migrations: decouple-picking, replan_of_sketch_id, planner_editing_sessions + last_activity, itemcountlog index, drive_time_reports ×2, waitMins, vehicle_day_pool); migrate; RBAC check that logistics users can reach the sketch routes. Solver box: NO ACTION — already serving the release code with per-ticket backups.

**4. Validation + week 1** — finalise a Tuesday plan on live and audit it; daily audits; sketch-planner.log as the finalise trail; fallback is the unchanged run planner (no rollback needed for planner misbehaviour); last-resort rollback = revert the merge commit (migrations are additive and safe to leave).