---
id: SN-005
slug: 2026-06-19
title: Status — 2026-06-19
project: target-tracker-byo-model-game-vision-perception-control
date: 2026-06-19
created: 2026-06-19T03:16:18Z
updated: 2026-06-19T03:16:18Z
rag:
  overall: green
  schedule: green
  scope: amber
highlights:
  - "MS-004 (Honest foundations) COMPLETE: project is under git; the single-player/offline guardrail is ENFORCED in code (every config attests scope; run scripts + a CI check reject violations; anti-cheat-evasion wording excised); added a reproducible runbook, pinned deps, responsible-use notice, and a risk register."
  - "MS-005 (Reproducible data spine) COMPLETE + adversarially HARDENED: built the pipeline that makes accuracy honest — held-out TEST split + trust gate, dataset QA gate, scene-diversity coverage verdict, per-dataset provenance, and content-hash versioning. A 2-pass adversarial review caught (twice) that the gates could silently report a fake-good number; rewrote them around one shared honest measure and re-verified."
  - "MS-006 BREAKTHROUGH (biggest project risk resolved): proved we can control a real commercial engine headless — injected kernel mouse input physically turns the Half-Life (Xash3D) camera, because uinput produces the raw input the engine reads (which synthetic input can't)."
  - "Pushed the whole codebase to GitHub (PRIVATE): github.com/AKAust/target-tracker — ~15 modules, docs, and visual proof (the autonomous agent playing at 100% accuracy; stock model 0 detections -> fine-tuned 0.91 on a Half-Life character; the uinput camera-turn)."
  - "Owner decisions captured as ADRs (8 total): beachhead = game-AI research/hobbyist tooling; scraping allowed for research/non-redistributable only; hardware passthrough deferred + purpose-scoped; MS-001/003 amended to reality. Session logged as TS-001."
next_two_weeks:
  - "Close the Xash/Half-Life control loop (MS-006): add kernel keyboard input for navigation (XTEST keys don't register in captured mode), calibrate look sensitivity, run detect->aim->fire on an NPC, then add an engine-authoritative hit log + a seeded hit-rate harness."
  - Capture DIVERSE Half-Life data so the trust gate passes -> a genuinely trustworthy mAP (not a pipeline-only number); then ratify the numeric reliability bar (ADR-005) and encode the ship gate (MS-007).
  - "Owner housekeeping: revoke the GitHub token pasted in chat + reissue with repo+workflow scope; pick a software LICENSE; then re-add the CI workflow file + push the agent-playing GIF (both held back by token scope)."
risks_summary: '"The remaining MS-006 work is loop-closure INTEGRATION (controlled navigation + sensitivity calibration), not a fresh unknown — the hard risk (can we control the engine at all) is now resolved. Finding: keyboard input via XTEST does NOT register in captured mode, so navigation needs kernel-uinput keyboard too. Security: a GitHub personal access token was pasted in chat and must be revoked. Recurring lesson banked: reliability/quality-gate code must be adversarially tested — it fooled us twice before we caught it."'
milestone_progress: "\"MS-002 hit. MS-004 + MS-005 complete and verified this session (minor transparent residuals: a LICENSE is still the owner's to pick; the adversarial gate-checks were done via review + manual validation and should be formalized as a committed test suite). MS-006 in progress — the captured-mode control breakthrough is proven; the full detect->aim->fire loop on Xash is the remaining integration. MS-001/003 amended to reality, pending owner-blessed reconciliation.\""
author:
  kind: agent
  name: claude-code
version: 1
---

# Status note — 2026-06-19

## Highlights (this week)

## Next two weeks

## Risks

## Issues

## Milestone progress
