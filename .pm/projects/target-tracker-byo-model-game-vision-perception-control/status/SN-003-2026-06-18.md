---
id: SN-003
slug: 2026-06-18
title: Status — 2026-06-18
project: target-tracker-byo-model-game-vision-perception-control
date: 2026-06-18
created: 2026-06-18T22:52:20Z
updated: 2026-06-18T22:52:20Z
rag:
  overall: green
  schedule: green
  scope: amber
highlights:
  - MS-002 (Pluggable ONNX detector) COMPLETE — marked hit. detectors.py defines a Detector ABC + Detection dataclass; OnnxDetector runs a YOLOv8 .onnx on the 5070 Ti via onnxruntime-CUDA at 254 fps; live.py is fully config-driven through .toml profiles (no hardcoded find_target); detections visualized on captured frames (det_out.png, vtest_det.mp4). All 4 acceptance criteria met.
  - "Live loop CLOSED end-to-end on a real GPU-rendered 3D game (Godot): the ONNX person-detector drives capture->detect->aim->shoot->hit at 100% on static and 95-100% on fast-moving person targets, via commit-to-target tracking + a person-sized hit zone. (MS-003 substance achieved — see scope note.)"
  - "NEW: capture->train->deploy lifecycle shipped (capture.py). Point it at a game + a detector; it records a YOLO dataset (images + auto-labels + data.yaml) of the game's OWN enemies, ready to fine-tune. Verified live against Godot (boxes land tightly on every target, conf 0.42-0.76); adversarially reviewed by a 5-agent workflow (0 critical / 2 major) with all confirmed fixes folded in (pixel-space box clipping, degenerate-box drop, frame.shape normalization, resume-safe numbering); unit-tested (clip/round-trip/resume all pass)."
  - "Detection generalizes to real footage: 4779 person detections across 795 frames of OpenCV vtest.avi pedestrians at 241 fps on the GPU."
  - "Docs consolidated: README quickstart now covers the BYO-model + capture path; docs/LIVE_LOOP.md and docs/DATA_CAPTURE.md document the full pipeline and the headless-Godot gotchas."
next_two_weeks:
  - "Decide Step 3 scope: control a real commercial pointer-lock FPS. Detection is ready; the remaining lift is the uinput captured-mode input path (relative-look + clicks that register under XInput2-raw), plus running a real game headless."
  - Owner to reconcile MS-001 / MS-003 acceptance criteria with the deliberate Godot + visible-cursor-reticle pivot (criteria currently name Xash3D + relative-mouse), then mark them hit or record the substitution as a decision.
risks_summary: Controlling a real commercial FPS (vs. our own Godot world) still needs the uinput captured-mode path sorted — we use a visible-cursor reticle precisely because headless relative-look is fragile. This is the one open technical unknown; perception itself is proven on real footage.
milestone_progress: MS-002 hit (4/4). MS-003 functionally complete (closed live loop on Godot, debug video, single-player guardrail in README+config) — 2 of 4 criteria deviate only in named tooling (Godot not Xash3D; reticle not relative-mouse), pending owner reconciliation. MS-001 GPU display verified (RTX 5070 Ti, :1) and real frames captured; Xash3D replaced by Godot as the engine.
author:
  kind: agent
  name: claude-code
version: 1
---

# Status note — 2026-06-18

## Highlights (this week)

## Next two weeks

## Risks

## Issues

## Milestone progress
