---
id: TS-003
slug: segmentation-score-account-level-the-conversion-layer-design
title: "Segmentation+score → account-level: the conversion-layer design (two-track grid + suggest→confirm machine)"
project: sales-segmentation-account-management
created: 2026-06-26T13:45:54Z
updated: 2026-06-26T16:17:00Z
decisions:
  - THREE LAYERS STAY DISTINCT. Segment (who they are) / Score (how good a prospect — potential, blind to spend) / Account-level (how we steward them). Account-level is the missing third layer and is NEVER silently derived from the other two. It also sits BESIDE — never replaces — the existing tierID (Diamond/Gold/Silver/Bronze value-grade) and accountType (Cash/Credit billing).
  - "THE ENGINE = a two-track Value×Potential grid, both axes in £/yr. POTENTIAL (X) = a static, anti-leakage curve from the score (never moved by spend). ACTUAL (Y) = realised hire-only revenue from ya_contracts. Levels: low actual+low potential → System-only (default floor); low actual+high potential → Incubation (the ~311 white-whale nursery); high actual → Account; high actual+high potential+repeat cadence → Strategic. Movement is VERTICAL (realised value climbs toward the customer's own fixed potential ceiling); share-of-wallet = actual÷potential; white-whale gap = vertical distance to the diagonal. This resolves Ben's 'move toward top-right' — it's 'up toward the ceiling', not lateral."
  - "NEW vs EXISTING is the two-track split. EXISTING (has REALISED hire history — a delivered contract, not just a future-dated booking) runs the full ladder on realised value. NEW (potential only) collapses onto the X-axis and is HARD-CAPPED at Incubation — a customer cannot be born Account/Strategic, they GRADUATE as real revenue accrues. Key fix: a forward-dated booking does NOT flip a new customer to 'existing' (that defeated the cap); it fires committed_fwd → big-fish alert + senior fast-track ownership NOW, but the durable level stays Incubation until revenue is realised. The single £60k one-off wedding routes to the 'Lifetime' follow-up process, NOT Strategic (Strategic now requires txn_count_12m >= 2 repeat cadence)."
  - "THE CONVERSION = a suggest→confirm state machine, not an algorithm that decides. System writes suggested_level only; a human sets confirmed_level (effective_level = COALESCE(confirmed, suggested)). States: suggested → proposed → owner_assigned → requirements_met → confirmed (+ rejected/demote). Each level has an ownership GATE before it is 'real': System-only → named pool steward (no individual salesID); Incubation → customerType-aware data capture (Corporate resolves companyType; Personal/no-domain uses the same set MINUS companyType so it can actually be satisfied); Account → semi-bespoke plan + manager approval; Strategic → bespoke plan + manager sign-off + a HARD per-senior-AM capacity cap (because ~311 candidates ≫ headcount). Transfer windows = quarterly review worklist (suggested≠confirmed, lapsed high-tier, split-ownership, disproven-whale). Big-fish = real-time manager alert on arrival."
  - "SEGMENT (95% missing) MODULATES, NEVER GATES a level — no rule depends on companyType, so the gap can't stall an assignment; segment is a fit-multiplier on potential (unknown ×1.0, never penalise missing data) and a Strategic tie-breaker. Enrichment is on-demand: resolving companyType becomes a requirement of the Incubation gate, so the engine itself becomes the segment-backfill engine. COMPETITORS are NOT excluded: a new customer_sales_scores.classification ENUM('mainstream','trade','disqualified') routes trade/sub-hire peers to a trade lane (System-only + trade pricing, or Account if high-volume) regardless of history; only genuinely not-events businesses are disqualified to System-only. GRAIN: the unit of stewardship is the economic account = email domain, rolled onto a deterministic 'home row' (merge primary, else highest-lifetime same-domain row) to avoid N-fold inflation across same-domain contacts; ownership conflicts (≥2 AMs on one domain) are FLAGGED, never silently overwritten."
  - "MODEL REFINED to a confidence-weighted blend (supersedes the hard new/existing two-track). Scoring+segmentation is UNIVERSAL (the 528 scored are mostly existing customers). Proposed level = blend: expected_value = alpha*realised_value + (1-alpha)*potential_value, where alpha in [0,1] is an evidence/confidence weight from delivered-order count + tenure + recency. New customer alpha~0 (proposal driven by score+segment + a weighted dab of initial quote value); established repeat alpha~0.8-0.9 (history dominates, score nudges). The 'born cannot be Strategic' rule becomes a consequence of alpha~0 + the realised-value gate, not a separate track. The potential-minus-realised GAP still drives Incubation/white-whale. ALL parameters (curve anchors, segment multipliers, alpha function, initial-quote weight beta, band cuts, white-whale SoW threshold, Strategic cap) live in a named/versioned PARAMETER SET with a what-if simulation screen that recomputes proposed levels over the scored base in-memory and shows the distribution shift + diff-vs-live before promoting a set to live. Design addendum in progress."
  - "ADD QUOTED VALUE as a third signal — revealed demand. A customer who quotes a lot but we are not winning still signals potential value (and a conversion problem worth sales attention). Quoted value sits between the cold web-lookup potential (they engaged) and realised won revenue (no money changed hands), so it feeds the POTENTIAL side of the blend: potential_value combines modelled (web score + segment) AND revealed (annualised deduped quoted demand); when revealed demand exists it is trusted over the web guess. This makes 'quotes lots / wins little' surface as a big potential-minus-realised GAP = white-whale / priority Incubation + a low win-rate (realised/quoted) conversion-problem flag (ties to the stopped-quotes / system-followup theme). CRITICAL: must DEDUPLICATE quotes before summing — same customer quoting multiple times around the same date for the SAME event (different item combos) is ONE opportunity, not N. Dedup key must include the HIRE/EVENT date (same created date + same hire date = duplicate variants -> collapse to one representative value e.g. converted-one-else-max; same created date + DIFFERENT hire dates = distinct opportunities -> keep). Naive SUM(quotesValue) massively overstates demand. Dedup applies equally to the last-300-new-quotes validation cohort."
  - "EMPIRICAL CALIBRATION from sandbox (2026-06-26), grounds the blend thresholds. (1) DEDUP: last-12m active+converted quotes = 27,393 rows -> 23,395 opportunities by (customer + event-date) = only ~14.6% inflation (moderate, not 2x); the copiedQuote flag carries £0 so it is UNUSED in practice -> dedup MUST be date-based (customer + hireStartDate +/-2 days, the existing UsefulFunctions convention), never the flag. (2) REALISED hire value distribution (trailing 12m, per customer): <£2k = 4,639 custs (85.6%), £2k-8k = 611, £8k-20k = 116, £20k-40k = 33, £40k+ = 22 (0.4%); ~5,421 active customers, ~£8.8M total. So band cuts of Account>=£8k (~171 custs) and Strategic>=£40k (~22 custs) are staffable as-is. Top 0.4% (£40k+) = ~18% of value; the <£2k mass = ~30% of value (so System-only still matters — justifies the steward/nurture investment). (3) The realised top is TINY (22 strategic, 171 account) -> growth must come from the white-whale/quoted-demand pool, which is exactly why the blend (quoted demand as potential) is needed, not a realised-only model."
  - "QUOTED-VALUE / WIN-RATE signal VALIDATED on real data. Among >=3-opportunity customers, win-rate ranges 0.03-1.00 at similar quoted values -> strongly discriminating. Canonical conversion-problem case: customer 8701 = 132 opportunities, 4 won (3% win rate), £69.6k quoted, ~£2k realised -> a realised-only model dumps them in System-only, but the blend (quoted demand -> potential) correctly elevates them to a high-priority Incubation / conversion-problem flag with a huge potential-minus-realised GAP. Other clear cases: 52903 (0.13, £53k), 5393 (0.25, £73k), 5194 (0.33, £105k), 59797 (0.00, £39k). Directionally ~£20M quoted vs ~£8.8M won hire in 12m => a large unconverted-demand prize the conversion lens targets. Use 8701 as the worked example in the addendum."
  - "BLEND REFINED (addendum, 2026-06-26, ~/Documents/P-0018-account-level-blend-addendum.md). CRITICAL CORRECTION: account level BANDS on a realised £ FLOOR (ground truth), NOT on alpha. Alpha only weights the steering value: expected_value = alpha*realised_blend + gamma*quote_signal + (1-alpha-gamma)*potential. An alpha-gate would have wrongly locked out loyal LOW-FREQUENCY flagships (a £50k/yr venue at 1 event/yr has alpha~0.36 but must be Strategic). Account requires realised_gate>=£8k AND a GRADUATION test (txn>=2 OR >=2 distinct years OR tenure>=9m OR lifetime>=£20k with a DELIVERED hire) so a single first order graduates through Incubation, never straight to Account. Strategic requires realised_gate>=£40k AND a repeat_signal. Three barriers between potential and a senior tier: alpha (shrinks steering value), the realised floor+graduation (band gate), human confirm+capacity cap."
  - "REALISED has TWO named bases, both pure contractTotal (never potential): realised_blend (recency-smoothed, steering) + realised_gate (multi-year annualised, hard floors) so a biennial £80k venue annualises to £40k and is not demoted in its off-year. Restored guards: LAPSED-HIGH-TIER (a dormant Diamond/Gold/VIP/keyAccount/£50k-lifetime account is NEVER silently dropped to System -> lapsed_review, hold prior level); committed_fwd DEMOTE-EXEMPTION (a material forward book holds the level); DELIVERED-not-converted (Account lifetime branch reads hireEndDate in the past, closing the forward-booking-as-history leak). Alpha = alpha_max*MIN(volume_factor from a recency-decayed DELIVERED-order count n_eff, tenure_factor); NULL-safe (0 at zero orders/tenure, no NaN); alpha_max 0.90 so a 10% potential prior always remains. Quote signal = MAX of ONE genuinely-engaged live quote (not SUM — resists padding), capped £30k + annualised; big-fish alerts read the RAW uncapped quote (unit-mismatch fix)."
  - "TUNING ARCHITECTURE (T-0479): a PURE AccountLevelEngine::computeRow(facts, params) is shared byte-identical between the nightly recompute (writes) and the what-if simulator (discards) — so the sim's band logic equals production. Params live in a versioned account_level_param_sets table (one JSON bundle, exactly one live, immutable-once-promoted, schema-validated on save: monotonic curve, floor<=cut etc.); customer_account_levels.compute_version stamps which set produced each row. A cached account_level_facts frame holds param-independent RAW windows + per-order ages so re-scoring the whole base per tweak is in-memory PHP. Screen = AccountLevelsController (sibling of SalesScoresController): constrained monotonic curve editor, distribution cards w/ Δ-vs-live, Value×Potential scatter, TWO diff matrices (suggested→suggested move count + separate confirmed-override panel so confirmed rows aren't double-counted), drill-down with plain-English band_reason. Hysteresis/dormancy/demote-exemption stay in the nightly job (need prior state), approximated+labelled in the sim."
  - "NEW ML SIGNAL idea (Ben, phone 2026-06-26): a 'quote-intrinsic potential' model that predicts white-whale / big-customer potential from the INITIAL QUOTE ALONE — the line ITEMS (product mix, quantities, premium-ness), the customer/delivery LOCATION (postcode/region), quote value + seasonality — with NO order history and NO web-domain score needed. It fills exactly the gap the demo exposed: brand-new leads (esp. the ~half on webmail/personal domains that can't be web-scored) currently get potential only from quote value, so a big new customer is invisible at first quote. Train supervised on historical first-quotes -> eventual outcome (did the customer become high-realised / high-LTV within N months), strictly leakage-guarded (features = first-quote-time only). It blends into the model as a THIRD potential source with its OWN tunable weight, dialled UP for new/unscored leads and DOWN where realised dominates. Designing via workflow; to be wired into the what-if demo as a tunable. Live example it would have caught: selainternational £24k first booking (Saudi PIF events producer) — big from the items+value, before any history."
  - "SEGMENT IS UNDER-USED in the account-level model (Austin, 2026-06-26). Currently the rich Industry→Company-type→Sub-type taxonomy (ADR-006/007, validated on 528) only feeds a segment_fit multiplier on potential — and that is neutralised to 1.0 pending enrichment — so segment does ~nothing live. Proposed fix: make segment a FIRST-CLASS TUNABLE driver via a 'segment profile' in the parameter store (T-0479), one row per industry/company-type carrying: repeat_likelihood (a PRIOR on repeat — a venue/caterer is structurally repeat, a one-off wedding/corporate is not — feeds the starting alpha + the Quick-vs-Lifetime conversion route; ties to M-005 outcome #7 Sam's repeat-from-quote formula), potential_multiplier / a SEGMENT-AWARE score→£ curve (fixes the demo under-calibration where caterlondon realises £119k on 'potential' £13.5k), default_conversion_process, optional threshold_overrides (M-005 #5: a venue ≠ a couple-of-large-orders managed account; venue = referral economics), and a qualification_set (M-005 #8: industry-specific qualification questions = the ownership gate). Segment is also already a feature in Ben's quote-intrinsic ML model. Net: segment becomes a dial in the what-if tool, honouring the segmentation work already done."
  - "LARGER SAMPLE SCORED (2026-06-26): 137 real new-customer domains from the last-300 quotes cohort, web-lookup, blind to spend. Result: tier A 33 (24%) / B 38 (28%) / C 66 (48%); classification mainstream 78 (57%) / DISQUALIFIED 54 (39%) / trade 4 / personal 1. KEY FINDINGS: (1) ~39% of fresh inbound quotes are NOT events businesses (Netflix, BMW dealership, Cineworld, care homes, running-shoe brand, architecture firms) -> the classification gate routes ~2-in-5 enquiries straight to System, a hard 'save sales time' number on real data. (2) 24% are A-tier white whales arriving in the new-quote stream (encoreglobal 98, eventconcept 96, wearesparks 94, broadwick 93 venue, National Theatre 85, Seasoned 85 caterer, TBA, INVNT) — invisible today because they look like a small first quote; validates Ben's quote-intrinsic model + universal new-lead scoring. (3) Trade detection worked: thorns 84, countessmarquees 81, platinumaceevents 73, kentonevents 68 — competitors that sub-hire -> trade lane, not growth plans. (4) The A-list splits cleanly into production agencies / venues / caterers / hotels-clubs-boats / trade — concrete evidence company-type should drive handling. Full scores saved local: ~/Documents/P-0018-new-quote-scores.{json,csv}, ready to import to customer_sales_scores (with classification) under T-0456."
actions:
  - text: "Full design doc (10 sections: model, level-assignment logic, new-vs-existing, 95%-missing-segment, conversion workflow, data model/schema, build sequencing, risks, 18 open decisions, edge cases) saved locally at ~/Documents/P-0018-account-level-design.md (design-only project — not committed). Produced via a 4-design panel + adversarial critique + red-team pass; top approach = Two-Track Scoring Engine (8/10)."
  - text: "WORKSHOP: take the design doc to the team and settle the 18 open decisions — most blocking are the £ thresholds (from a real percentile study), the Strategic per-senior-AM capacity cap number, the named SYSTEM/trade pool steward (+ who funds the Phase-4 nurture engine it depends on), and the domain-ownership cascade rule (who wins when 2 AMs already own contacts at one agency). These gate Phase 2."
    ticket: T-0457
  - text: "BUILD (post-approval) per section 7: Phase 0 = add customer_sales_scores.classification + seed from funnel flags (T-0456), indexed ya_customers.email_domain, roleID→seniority stub. Phase 1 MVP = customer_account_levels + log tables, nightly recompute job (hire-only contractTotal, domain-rolled to home row, committed_fwd + pending_quote_value, NULL-safe momentum, status_stage, white-whale, big-fish, hysteresis), my-accounts level badge + confirm/override + audit, Strategic RBAC + capacity cap. Phase 2 = full state machine + gates + transfer-window job + parameterised big-fish mailer. Phase 3 = enrichment-on-demand (T-0473/4/5). Phase 4 = the (currently unbuilt) nurture engine."
    ticket: T-0457
  - text: "VALIDATION COHORT: score the last ~300 NEW quotes through the model (web-lookup score + segment + initial quote value) to get real NEW-customer data — the 528 scored base is mostly EXISTING customers, so we currently have no new-customer ground truth. This (a) shows where genuinely-new customers land under the blend (Incubation vs System vs big-fish distribution), (b) tests the alpha~0 + initial-quote (beta) path, and (c) since we can later see which of those quotes converted, gives a feedback signal to calibrate the potential curve + alpha. Feed this cohort into the what-if simulation tool as a second population alongside the existing scored base. Runnable as a data task once the scoring command is productionised (T-0456)."
    ticket: T-0456
  - text: "RECOMPUTE-LOGIC CORRECTIONS exposed by the sandbox data: (1) ya_customers.totalContracts is UNRELIABLE for new-vs-existing — established quoters (e.g. 5249 tastingplates with 91 opps, 5534) show totalContracts=0/NULL. The track test MUST use realised ya_contracts history (a delivered, unconverted=0, contractType=1 contract with hireEndDate in the past) or prior-quote existence, NOT totalContracts. (2) Filter £0.00 quotes (empty/abandoned) and INTERNAL quotes on the yahire.com domain (e.g. customer 64358) before computing demand. (3) New-quote cohort is heavy on webmail/personal domains (gmail/hotmail/icloud/yahoo/outlook, ~1/3+) and shared domains (.ac.uk/.org.uk) -> the personal/no-domain track + quote-value proxy is a MAJORITY path, not an edge case; also handle typos like gamil.com. (4) Same-day multi-combo dups are real in the cohort (e.g. me.com cust 60405: 4 quotes same event date £1014/£415/£2441/£3427 = one opportunity -> take max)."
    ticket: T-0457
  - text: "STAKEHOLDER DEMO decided (grounds the stuck decisions from M-005 outcome #9 'spent a long time on what to do with qualified customers... parameters not defined' + April's #1 issue = Visibility): build an INTERACTIVE HTML prototype (a clickable preview of the T-0479 what-if tool) seeded with FULLY-REAL data — a curated ~30-customer spanning sample (strategic / account / system-small / conversion-problem incl. 8701 / new-lead / high-performer buckets). Stakeholders drag the Account/Strategic/alpha/quote-weight thresholds and watch customers re-band live on a Value×Potential scatter + distribution, so the finer-point threshold decisions become decidable in the room. Build it via a workflow (build + adversarially verify every displayed number against the blend formula) once the sandbox query returns. Self-contained HTML, screen-shareable."
    ticket: T-0479
  - text: "STAKEHOLDER DEMO BUILT + verified: ~/Documents/P-0018-account-level-demo.html — self-contained interactive prototype (25KB, no internet), 27 REAL sandbox customers across every level. Engine run in Node to verify: default-threshold distribution = Strategic 5 / Account 8 / Incubation 2 / Triage 6 / System 5 / Excluded 1. Drag Account/Strategic/Incubation/β/α-half-life/k sliders → live re-band + 'N customers moved X→Y' diff + Value×Potential scatter. Headline real cases it demonstrates: gpj.com (score 95, £29k realised, sow 0.30) + stratacreate (score 93, £12k realised, sow 0.12) = Account-but-white-whale+conversion-gap; drpgroup/corporate-events = conversion gap (quoted >> realised, low win); selainternational £24k BOOKED-not-delivered = Triage/big-fish not auto-promoted (forward-booking guard); yahire.com = Excluded (internal classification gate); smartgroupltd twice = the home-row/economic-account grain. Banding gates on realised £ floor (not α). NOTE the score→£ curve is visibly under-calibrated for proven top accounts (caterlondon £119k realised, score 72 → potential only £13.5k) — exactly the calibration refit flagged as a workshop decision; banding is unaffected because it uses realised_gate."
    ticket: T-0479
side_quests: []
problem: "We have an initial scoring model (web-lookup, blind to spend, A/B/C) and an initial segmentation (industry) model, but no designed way to get a customer from there to ONE of the four account levels (System / Incubation / Account / Strategic), nor a process for how Sales actually CONVERTS a flagged customer into being that level. Two hard realities had to be handled: (1) existing customers have real order history (ya_contracts) but a brand-new customer has none — only potential; (2) segment is missing on ~95% of accounts and the only backfill (web-scrape) is keyed by email domain, so personal/webmail customers can't be segmented that way. Ben also corrected that peer/competitor suppliers must NOT be excluded — some sub-hire from us, so they are a legitimate trade segment."
phase: build
version: 23
---

# TS-003: Segmentation+score → account-level: the conversion-layer design (two-track grid + suggest→confirm machine)

## Problem

## Success criterion

## Decisions

## Actions

## Side-quests
